1.	#ifndef 和 #define 可以防止头文件重复包含，和里面声明的是变量还是函数无关。

2.	声明了 extern int a;，extern int d;，extern int c[5];，
	告诉编译器这些变量是全局的，但是不在这里定义，请去别的地方找。
	extern 是告诉编译器，这些变量在别的 .cpp 文件里定义，不要分配内存，只是声明一下。

3.	链接阶段会把这些引用和定义进行匹配。